interface:
  display_name: "Markdown DOCX Report Pipeline"
  short_description: "Markdown to formatted Word reports"
  default_prompt: "Use markdown-docx-report-pipeline to create a Markdown report or convert Markdown into a formatted Word document, preserving the reference document format and validating the output."
